// Generated by the reflaxe TypeScript target. Do not edit.

import { GlyphMetrics } from "./GlyphMetrics.ts";

export class VectorSort {
  public static byCodePoint(records: GlyphMetrics[]): GlyphMetrics[] {
    for (let write = 1, count = records.length; write < count; write += 1) {
      const record = records[write]!;
      const key = record.codePoint;
      let read = write - 1;
      while (read >= 0 && records[read]!.codePoint > key) {
        records[read + 1] = records[read]!;
        read -= 1;
      }
      records[read + 1] = record;
    }
    return records;
  }
}
