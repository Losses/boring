package tests;

/** Expected diagnostic text for the three invalid marker forms. */
class StaticExtensionProbes {
	public static final instanceMarkerError = "top-level markers accept static functions with a concrete receiver only";
	public static final extensionTypeParameterError = "top-level markers accept static functions with a concrete receiver only";
	public static final extensionNoParametersError = "top-level markers accept static functions with a concrete receiver only";
}
