/**
 * Compiles with `-lib reflaxe` to prove the pinned framework resolves and
 * its initialization macro runs. This is an environment check; it is not
 * part of the codec test suites.
 */
class Smoke {
	static function main() {}
}
